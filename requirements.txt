# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
aiosqlite>=0.20.0

# LLM
anthropic>=0.40.0

# x402 payment signing (EIP-3009 authorization).
# The x402 protocol flow is implemented directly against the facilitator;
# eth-account provides the signer used in live mode.
eth-account>=0.11.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
